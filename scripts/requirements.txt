# RESR Property OS - Python Utilities & Data Extraction Requirements

# TypeSafe AI Python SDK for Jev / System One models
typesafe-sdk>=0.1.0

# HTTP Client & HTML Parsing (for portal scraping / nehnutelnosti.sk)
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=5.1.0

# Environment variables & configuration
python-dotenv>=1.0.1

# Data validation & Typed Schemas
pydantic>=2.7.0

# PostgreSQL database adapter (for direct DB queries/migrations)
psycopg2-binary>=2.9.9
